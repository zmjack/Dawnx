@page
@model Dawnx.AspNetCore.LiveAccountUtility.Pages.Users.IndexModel

@{
    ViewBag.Title = "Users";

    var __IdentityUser__ = new Microsoft.AspNetCore.Identity.IdentityUser();
    var Users = ViewBag.Users as IEnumerable<Microsoft.AspNetCore.Identity.IdentityUser>;
    var LiveRoles = ViewBag.LiveRoles as IEnumerable<Dawnx.AspNetCore.LiveAccount.LiveRole>;
    var LiveUserRoles = ViewBag.LiveUserRoles as IEnumerable<Dawnx.AspNetCore.LiveAccount.LiveUserRole>;
}

<h2>LiveUser</h2>

<p>
    <a href="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => __IdentityUser__.UserName)</th>
            @foreach (var role in LiveRoles)
            {
                <th>@role.Name</th>
            }
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Users)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => user.UserName)</td>
                <td>
                    @foreach (var liveRole in LiveRoles)
                    {
                        if (LiveUserRoles.Any(x => x.User == user.Id && x.Role == liveRole.Id))
                        {
                            <text>✔</text>
                        }
                        else { }
                    }
                </td>
                <td>
                    <a href="Edit?Id=@user.Id">Edit</a> |
                    <a href="Details?Id=@user.Id">Details</a> |
                    <a href="Delete?Id=@user.Id">Delete</a> |
                </td>
            </tr>
        }
    </tbody>
</table>
